org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official

# URL of the update manifest baked into BuildConfig.UPDATE_URL.
# Release builds refuse to build while this is the placeholder.
# Override locally without editing this file:
#   ./gradlew -Primo.updateUrl=http://192.168.1.10:8000/update.json installDebug
rimo.updateUrl=https://example.invalid/update.json
