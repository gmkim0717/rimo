<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Full-screen dark theme. TV apps never show a status or action bar. -->
    <style name="Theme.Rimo" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <!-- Button with an explicit, high-contrast focus state (never rely on the system highlight). -->
    <style name="Widget.Rimo.FocusButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:background">@drawable/bg_button_focus</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingStart">28dp</item>
        <item name="android:paddingEnd">28dp</item>
        <item name="android:minHeight">52dp</item>
        <item name="android:focusable">true</item>
        <!-- Boxes have no touch, but emulators and the odd touch-capable box do: without this,
             requestFocus() is ignored in touch mode and the initial focus border never shows. -->
        <item name="android:focusableInTouchMode">true</item>
    </style>
</resources>
